'use strict';

/**
 * active-gallery-page-slider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::active-gallery-page-slider.active-gallery-page-slider');
