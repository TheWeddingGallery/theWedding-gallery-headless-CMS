'use strict';

/**
 * gallery-page-slider router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::gallery-page-slider.gallery-page-slider');
