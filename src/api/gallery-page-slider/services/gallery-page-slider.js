'use strict';

/**
 * gallery-page-slider service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::gallery-page-slider.gallery-page-slider');
